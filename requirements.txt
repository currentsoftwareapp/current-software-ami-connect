certifi==2026.2.25
charset-normalizer==3.4.7
idna==3.15
requests==2.33.1
urllib3==2.7.0
black==26.3.1
PyYAML==6.0.3
boto3==1.42.84
paramiko==3.5.1
oracledb==3.4.2
sshtunnel==0.4.0
typer==0.24.1
snowflake-sqlalchemy==1.9.0
python-dotenv==1.2.2
# Installs with constraints for Airflow.
# Keep --constraint on its own line so pip applies it globally; appended to a
# package line it is silently ignored.
# This is a LOCAL FORK of Apache's constraints-3.2.1/constraints-3.12.txt with three
# deviations to address CVEs (urllib3 -> 2.7.0, amazon provider -> 9.28.0, idna ->
# 3.15). See the header in that file for details and how to refresh it.
--constraint constraints-3.2.1.txt
apache-airflow==3.2.1
apache-airflow-providers-snowflake==6.12.1
apache-airflow-providers-amazon==9.28.0
apache-airflow-providers-fab==3.6.1
apache-airflow-providers-standard==1.12.3

# Running production Airflow
psycopg2-binary==2.9.11
